import { RepateDirective } from './repate.directive';

describe('RepateDirective', () => {
  it('should create an instance', () => {
    const directive = new RepateDirective();
    expect(directive).toBeTruthy();
  });
});
