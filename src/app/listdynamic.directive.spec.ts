import { ListdynamicDirective } from './listdynamic.directive';

describe('ListdynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new ListdynamicDirective();
    expect(directive).toBeTruthy();
  });
});
