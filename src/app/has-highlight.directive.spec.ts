import { HasHighlightDirective } from './has-highlight.directive';

describe('HasHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HasHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
